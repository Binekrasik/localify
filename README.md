# localify

> [!WARNING]
> This is a hobby project. I do NOT guarantee any maintenance or responsibility.

## Running
1. Clone the repo
2. `cd` into its directory
3. Install dependencies using `bun i` or `bun install`
4. Run `bun dev`
5. Finally, localify should be available on `http://localhost:3000`
